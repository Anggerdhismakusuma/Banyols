<?xml version="1.0" encoding="utf-8"?>
<!-- 1. WAJIB pakai DrawerLayout sebagai pembungkus paling luar -->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BooksActivity">

    <!-- 2. Konten Utama (Gunakan LinearLayout di dalam sini) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/bg_main">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#FFF9ED"
            app:contentInsetStartWithNavigation="0dp"
            app:layout_collapseMode="pin"
            app:navigationIcon="@drawable/ic_menu">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/noto_serif"
                android:text="D'Classics"
                android:textColor="#570013"
                android:textSize="20sp"
                android:textStyle="italic|bold" />
        </androidx.appcompat.widget.Toolbar>

        <!-- TAB -->
        <LinearLayout
            android:id="@+id/tabContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginHorizontal="20dp"
            android:layout_marginTop="16dp">

            <TextView
                android:id="@+id/tabFiction"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="FICTION"
                android:gravity="center"
                android:padding="12dp"
                android:textColor="@color/text_maroon"
                android:textStyle="bold"
                android:background="@drawable/tab_indicator_line"/>

            <TextView
                android:id="@+id/tabNonFiction"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="NON-FICTION"
                android:gravity="center"
                android:padding="12dp"
                android:textColor="@color/text_grey"
                android:textStyle="bold"/>
        </LinearLayout>

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPagerBooks"
            android:layout_width="match_parent"
            android:padding="20dp"
            android:layout_height="0dp"
            android:layout_weight="1"/>
    </LinearLayout>

    <!-- 3. Sidebar Menu (NavigationView) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#FFF9ED"
        app:headerLayout="@layout/nav_header"
        app:itemIconTint="#8C6F71"
        app:itemShapeFillColor="#F9F6EF"
        app:itemTextColor="#51606B"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>